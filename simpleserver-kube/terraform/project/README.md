# Instructions

1. `source` the environment variables file.
2. `init.sh`
3. terraform plan
4. terraform apply
5. `./set-owner.sh` <environment>

`set-owner.sh` script just binds the account with role owner to the project - makes it easier to browse resources using `gcloud` or GCP Portal.
